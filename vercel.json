{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && ./node_modules/.bin/vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
